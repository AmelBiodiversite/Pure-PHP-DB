<!DOCTYPE html>
<html lang="fr">
    <head>
        <!-- =====================================================
             MARKETFLOW PRO - HEADER LAYOUT
             ===================================================== 
             
             Version : 2.0
             Dernière modification : <?= date('d/m/Y') ?>
             
             AMÉLIORATIONS :
             - Utilisation des constantes CSS_URL et JS_URL
             - Support des notifications toast modernes
             - Conversion automatique des messages flash PHP
             - Chargement optimisé des ressources
             
             DÉPENDANCES :
             - config/config.php (définit CSS_URL, JS_URL)
             - public/css/style.css (styles principaux)
             - public/css/notifications.css (système toast)
             - public/js/app.js (composants UI)
             - helpers/functions.php (getFlashMessage, e)
             ===================================================== -->
        
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="MarketFlow Pro - La marketplace pour créateurs digitaux">
        
        <!-- Titre dynamique de la page -->
        <title><?= isset($title) ? e($title) . ' - ' : '' ?>MarketFlow Pro</title>
        
        <!-- =====================================================
             STYLES CSS
             ===================================================== -->
        
        <!-- Style principal du site -->
        <!-- Chemin : public/css/style.css -->
        <link rel="stylesheet" href="<?= CSS_URL ?>/style.css">
        
        <!-- Système de notifications toast modernes -->
        <!-- Chemin : public/css/notifications.css -->
        <!-- Permet d'afficher les messages flash en notifications animées -->
        <link rel="stylesheet" href="<?= CSS_URL ?>/notifications.css">
        
        <!-- =====================================================
             JAVASCRIPT PRINCIPAL
             ===================================================== -->
        
        <!-- Script principal avec composants (Dropdown, Menu, etc.) -->
        <!-- Chemin : public/js/app.js -->
        <!-- Charge en premier pour initialiser les composants de base -->
        <script src="<?= JS_URL ?>/app.js"></script>
        
        <!-- Favicon -->
        <link rel="icon" href="<?= IMG_URL ?>/favicon.ico">
    </head>
    
    <body>
        <!-- =====================================================
             NAVIGATION PRINCIPALE
             ===================================================== -->
        <nav class="bg-white shadow-md sticky top-0 z-50">
            <div class="container mx-auto px-4">
                <div class="flex items-center justify-between h-16">
                    
                    <!-- =====================================================
                         LOGO & NAVIGATION GAUCHE
                         ===================================================== -->
                    <div class="flex items-center space-x-8">
                        <!-- Logo -->
                        <a href="/" class="flex items-center space-x-2">
                            <div class="w-10 h-10 bg-gradient-to-br from-primary-600 to-primary-800 rounded-lg flex items-center justify-center">
                                <span class="text-white font-bold text-xl">M</span>
                            </div>
                            <span class="text-xl font-bold text-gray-900">MarketFlow</span>
                        </a>
                        
                        <!-- Menu principal -->
                        <div class="hidden md:flex space-x-6">
                            <a href="/" class="text-gray-700 hover:text-primary-600 transition">Accueil</a>
                            <a href="/products" class="text-gray-700 hover:text-primary-600 transition">Produits</a>
                            <a href="/categories" class="text-gray-700 hover:text-primary-600 transition">Catégories</a>
                        </div>
                    </div>
                    
                    <!-- =====================================================
                         NAVIGATION DROITE (Auth & Panier)
                         ===================================================== -->
                    <div class="flex items-center space-x-4">
                        
                        <!-- Barre de recherche (Desktop) -->
                        <div class="hidden md:block">
                            <form action="/search" method="GET" class="relative">
                                <input 
                                    type="text" 
                                    name="q" 
                                    placeholder="Rechercher un produit..." 
                                    class="w-64 px-4 py-2 pr-10 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                                >
                                <button type="submit" class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-primary-600">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                                    </svg>
                                </button>
                            </form>
                        </div>
                        
                        <!-- Panier -->
                        <a href="/cart" class="relative text-gray-700 hover:text-primary-600 transition">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"/>
                            </svg>
                            <?php 
                            // Compteur panier (si items présents)
                            $cartCount = $_SESSION['cart_count'] ?? 0;
                            if ($cartCount > 0): 
                            ?>
                                <span class="absolute -top-2 -right-2 bg-red-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
                                    <?= $cartCount ?>
                                </span>
                            <?php endif; ?>
                        </a>
                        
                        <?php if (isLoggedIn()): ?>
                            <?php $currentUser = getCurrentUser(); ?>
                            
                            <!-- Menu utilisateur connecté -->
                            <div class="relative" data-dropdown>
                                <button class="flex items-center space-x-2 text-gray-700 hover:text-primary-600 transition">
                                    <div class="w-8 h-8 bg-primary-600 rounded-full flex items-center justify-center text-white font-semibold">
                                        <?= strtoupper(substr($currentUser['username'], 0, 1)) ?>
                                    </div>
                                    <span class="hidden md:inline"><?= e($currentUser['username']) ?></span>
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                                    </svg>
                                </button>
                                
                                <!-- Dropdown menu -->
                                <div class="dropdown-menu hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg py-2 border border-gray-200">
                                    <a href="/account" class="block px-4 py-2 text-gray-700 hover:bg-gray-100 transition">Mon compte</a>
                                    <a href="/orders" class="block px-4 py-2 text-gray-700 hover:bg-gray-100 transition">Mes commandes</a>
                                    
                                    <?php if ($currentUser['role'] === 'seller'): ?>
                                        <hr class="my-2">
                                        <a href="/seller/dashboard" class="block px-4 py-2 text-gray-700 hover:bg-gray-100 transition">Dashboard vendeur</a>
                                        <a href="/seller/products" class="block px-4 py-2 text-gray-700 hover:bg-gray-100 transition">Mes produits</a>
                                    <?php endif; ?>
                                    
                                    <?php if ($currentUser['role'] === 'admin'): ?>
                                        <hr class="my-2">
                                        <a href="/admin" class="block px-4 py-2 text-primary-600 hover:bg-gray-100 transition font-semibold">Administration</a>
                                    <?php endif; ?>
                                    
                                    <hr class="my-2">
                                    <a href="/logout" class="block px-4 py-2 text-red-600 hover:bg-red-50 transition">Déconnexion</a>
                                </div>
                            </div>
                        <?php else: ?>
                            <!-- Boutons connexion/inscription -->
                            <a href="/login" class="text-gray-700 hover:text-primary-600 transition">Connexion</a>
                            <a href="/register" class="bg-primary-600 text-white px-4 py-2 rounded-lg hover:bg-primary-700 transition">Inscription</a>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </nav>
        
        <!-- =====================================================
             SYSTÈME DE NOTIFICATIONS TOAST
             ===================================================== 
             
             Convertit automatiquement les messages flash PHP 
             en notifications toast modernes par notifications.js
             
             Attributs data-* utilisés pour la conversion :
             - data-flash-message : Le message à afficher
             - data-flash-type : Type (success, error, warning, info)
             ===================================================== -->
        <?php 
            $flash = getFlashMessage();
            if ($flash): 
        ?>
        <div data-flash-message="<?= e($flash['message']) ?>" 
             data-flash-type="<?= $flash['type'] ?>" 
             style="display: none;">
            <!-- Ce message sera converti en toast par notifications.js au chargement de la page -->
        </div>
        <?php endif; ?>
        
        <!-- =====================================================
             DÉBUT DU CONTENU PRINCIPAL
             ===================================================== -->
        <!-- Le contenu de chaque page sera inséré ici via les vues -->
